Critical/enhanced portions of django-json-rpc are already built into
the CRDS client and don't need to be installed.  This is provided as
reference.

Parsley is used by crds.certify to check for duplicate entries in
mappings.  Checking is optional and fails nicely if parsely is not
installed.

