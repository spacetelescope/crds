#! /usr/bin/env pysh
#-*-python-*-

if len(sys.argv) == 1:
    from crds.hst import INSTRUMENTS
else:
    INSTRUMENTS = sys.argv[1:]

for instr in INSTRUMENTS:
    % python convert_index.py ../${instr}/sources/${instr}_index.xml  --generate-context-rmap
    % mv ${instr}.imap ../mappings/hst/hst_${instr}.imap

