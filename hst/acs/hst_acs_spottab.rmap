header = {
    'mapping' : 'reference',
    'observatory' : 'HST',
    'instrument' : 'ACS',
    'reftype' : 'SPOTTAB',
    'parkey' : (('DETECTOR',), ('DATE-OBS', 'TIME-OBS')),
}

selector = Match({
    ('HRC',) : UseAfter({
        '2002-03-01 00:00:00' : 'r3301467j_csp.fits',
    }),
})
