#! /bin/tcsh

cd `dirname $0`

./clean

foreach dir (lib hst client ../CRDS_server)
    if (-e $dir) then
    	echo "Installing $dir"
		(cd $dir;  python setup.py install --force)  > /dev/null
    else
		echo "Skipping $dir"
    endif
end

./clean

