"""This module is intended to present a programmatic API to the
the CRDS submit functionality,  particularly as it pertains to the
extended form which includes ReDCaT metadata and forward functions.
"""

def submit():
    """
    """
    # login

    certify_files(abs_paths, context=context, dump_provenance=True,
                  observatory=observatory, compare_old_reference=True,
                  run_fitsverify=True, check_rmap=True, check_sha1sums=True)

    # filecopy (web post?)

    # meta-post

    # monitor / wait-for-complete?
